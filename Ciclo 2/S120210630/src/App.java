public class App {
    public static void main(String[] args) throws Exception {
        //CLASE STRING

        //CLASE MATH

        //CLASE WRAPPER (ENVOLTORIO)

        //CLASE SYSTEM

        //GIT & GITHUB
        /*
        Algunos comandos importantes en orden para subir los archivos a GITHUB son:
        1) git --version (Da la versión)
        2) git init (Initialized empty Git repository in C:/Users/FERNANDO TORO/Documents/MisionTic2022/.git/)
        3) git status (Muestra los archivos o carpetas en la dirección inicializada)
        4) git add . (carga los archivos)
        5) git commit -m 'Archivos MisionTic2022' (sube todos los archivos a GITHUB pero se debe configurar usuario y email con )
        git config --global user.name "Your Name" y git config --global user.email
        6) git remote add origin https://github.com/ftorob/MisionTICS120210639-G09.git (ruta de repositorio)
        7) git push -u origin master (cargue automático)
        Para poder subir todos los archivos fui a GITHUB --> SETTINGS --> REPOSITOIRES --> Cambie "MAIN" por "MASTER"
        para poder arreglar el error del push "error: src refspec main does not match any
        error: failed to push some refs to 'https://github.com/ftorob/MisionTICS120210639-G09.git'"
        */
    }
}
